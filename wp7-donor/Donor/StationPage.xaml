<phone:PhoneApplicationPage 
    x:Class="Donor.StationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="False" Loaded="PhoneApplicationPage_Loaded">

    <!--LayoutRoot contains the root grid where all other page content is placed-->
    <Grid x:Name="LayoutRoot">
        <controls:Panorama>
            <controls:Panorama.Title>
                <TextBlock Text="{Binding Title}" Margin="0,80,0,0" FontSize="30"/>
            </controls:Panorama.Title>
            <!--Panorama item one-->
            <controls:PanoramaItem Header="Основное">
                <Grid>
                    <StackPanel>
                        <TextBlock TextWrapping="Wrap" Text="Адрес" Style="{StaticResource PhoneTextTitle2Style}" />
                        <TextBlock TextWrapping="Wrap" Text="{Binding Adress}"/>
                        <TextBlock TextWrapping="Wrap" Text="Телефон" Style="{StaticResource PhoneTextTitle2Style}"/>
                        <TextBlock TextWrapping="Wrap" Text="{Binding Phone}"/>
                        <TextBlock Style="{StaticResource PhoneTextTitle2Style}" Text="{Binding RegionalRegistrationString}" TextWrapping="Wrap"/>
                        <TextBlock Text="Сайт" Style="{StaticResource PhoneTextTitle2Style}" TextWrapping="Wrap"/>
                        <TextBlock TextWrapping="Wrap" Text="{Binding Url}"/>
                    </StackPanel>
                </Grid>
            </controls:PanoramaItem>

            <!--Panorama item two-->
            <controls:PanoramaItem Header="Для доноров">
                <Grid>
                    <StackPanel>
                        <TextBlock TextWrapping="Wrap" Text="Время приема" Style="{StaticResource PhoneTextTitle2Style}" />
                        <TextBlock TextWrapping="Wrap" Text="{Binding ReceiptTime}"/>
                        <TextBlock TextWrapping="Wrap" Text="Проезд" Style="{StaticResource PhoneTextTitle2Style}"/>
                        <TextBlock TextWrapping="Wrap" Text="{Binding Transportaion}"/>
                        <TextBlock TextWrapping="Wrap" Text="Кровь для" Style="{StaticResource PhoneTextTitle2Style}"/>
                        <TextBlock Text="{Binding BloodFor}"/>
                    </StackPanel>
                </Grid>
            </controls:PanoramaItem>

            <controls:PanoramaItem Header="Объявления">
                <Grid/>
            </controls:PanoramaItem>

            <controls:PanoramaItem Header="Отзывы">
                <Grid/>
            </controls:PanoramaItem>
        </controls:Panorama>
    </Grid>

    <!--Panorama-based applications should not show an ApplicationBar-->

</phone:PhoneApplicationPage>