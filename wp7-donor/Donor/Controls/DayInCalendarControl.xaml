<UserControl x:Class="Donor.Controls.DayInCalendarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="56" d:DesignWidth="56">
    
    <Grid x:Name="LayoutRoot" Margin="2">
        <Border BorderThickness="2" BorderBrush="Gray" Name="MainBorder" Background="{Binding BgColor}">
            <StackPanel>
                <Image Height="27" Width="27" Name="DayImage" Source="{Binding ImagePath}"/>
                <TextBlock Margin="2" Foreground="{Binding TextColor}" Text="{Binding DayNumber}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
